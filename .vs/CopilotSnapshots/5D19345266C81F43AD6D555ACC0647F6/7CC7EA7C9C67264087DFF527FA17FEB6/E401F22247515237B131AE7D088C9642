using Microsoft.Agents.Protocols.Primitives;
using Microsoft.Agents.Handlers; // Trying to find where ActivityHandler lives if possible
using System.Threading;
using System.Threading.Tasks;
using System.Collections.Generic;

namespace MeetingBreakout.WebApp
{
    // The user requested an "agent" using Agents SDK.
    // We will attempt to use ActivityHandler if available, or IBot.
    public class BreakoutAgent : ActivityHandler
    {
        protected override async Task OnMembersAddedAsync(IList<ChannelAccount> membersAdded, ITurnContext<IConversationUpdateActivity> turnContext, CancellationToken cancellationToken)
        {
            foreach (var member in membersAdded)
            {
                if (member.Id != turnContext.Activity.Recipient.Id)
                {
                    // Logic to check participant role and send card will go here
                    // verifying non-organizer participant
                }
            }
        }
    }
}
